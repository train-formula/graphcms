
extend type Query {
    exercise(id: ID!): Exercise
}

type Exercise {

    id: ID!

    createdAt: Time!
    updatedAt: Time!

    trainerOrganizationID: ID!

    name: String!
    description: String!

    videoURL: String

    # Fetchers
    tags: [Tag!]

}
