type Query {
    health: String!
}

type Mutation {
    health: String!
}

type PageInfo {
    startCursor: String!
    endCursor: String!
    hasNextPage: Boolean!
}

type Unit {
    id: ID!
    unit: String
    indicatesTime: Boolean
}

# RFC 3339 datetime string
scalar Time